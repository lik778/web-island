version: '3'
services:
  db:
    image: mysql:5.7
    environment:
      - MYSQL_DATABASE=taxonomy
      - MYSQL_ROOT_PASSWORD=baixing123
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql
    restart: always
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db

volumes:
  db-data:
